diff a/apps/server/src/playback/service.ts b/apps/server/src/playback/service.ts	(rejected hunks)
@@ -291,11 +302,18 @@ export function resolveCastCommand(
       command: existing,
       playback: playbackState(db, profileId, ownerDeviceId),
     };
-  if (input.status === "accepted" && !input.confirmed)
-    throw new Error("CAST_CONFIRMATION_REQUIRED");
 
   if (input.status === "accepted") {
     const before = playbackState(db, profileId, ownerDeviceId);
-    startCast(db, profileId, ownerDeviceId, input.confirmed!);
-    const action = existing.command.action;
-    if (
+    if (before.mode === "cast") {
+      if (!input.confirmed) throw new Error("CAST_CONFIRMATION_REQUIRED");
+      startCast(db, profileId, ownerDeviceId, input.confirmed);
+    }
+    const action = existing.command.action;
+    if (
+      before.mode === "cast" &&
+      input.confirmed &&
+      (action === "seek" ||
+        action === "skip-forward" ||
+        action === "skip-backward")
+    ) {
