name: Validate Cast recovery lifecycle refinements

on:
  push:
    branches:
      - fix/cast-session-recovery-0.4.35

permissions:
  contents: write

jobs:
  validate:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.15.0 --activate
      - name: Apply Cast recovery refinements
        run: python tools/refine_cast_session_recovery.py
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate Android TypeScript and Jest
        run: |
          pnpm --filter @podwaffle/android typecheck
          pnpm --filter @podwaffle/android exec jest --runInBand
      - name: Prebuild Android
        run: pnpm --filter @podwaffle/android exec expo prebuild --platform android --no-install
      - name: Validate native Cast recovery
        run: |
          cd apps/android/android
          ./gradlew :podwaffle-media:testDebugUnitTest :app:compileReleaseKotlin --no-daemon
      - name: Check patch formatting
        run: git diff --check
      - name: Commit validated refinement
        run: |
          rm tools/refine_cast_session_recovery.py
          rm .github/workflows/validate-cast-session-recovery-refinement.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+Invertee@users.noreply.github.com"
          git add -A
          git commit -m "fix(android): harden Cast sender reattachment [skip ci]"
          git push
